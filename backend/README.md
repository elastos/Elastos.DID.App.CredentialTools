# Credential toolbox backend

## Initial setup

- clone config/env-secret.template into config/env-secret.ts, and configure the required information
- npm install -D
- npm run import-types
- npm run start